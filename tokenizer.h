#ifndef TOKENIZER_H
#define TOKENIZER_H

/**
 * Interface for tokenizer function. Tokenizes the given string into basic words, special characters, and words enclosed in special characters
 */
char **tokenizer(char *input);

#endif